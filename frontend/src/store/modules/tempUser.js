const state = {
    // 搜索内容索引
    user: {
        userID: 1
    }
}

const getters = {
  user: state => state.user,
}

const mutations = {

}

const actions = {

}

export default {
  state,
  getters,
  actions,
  mutations
}
